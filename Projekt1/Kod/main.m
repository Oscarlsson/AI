%% This file runs the different algos on some datasets...

load data.mat

% TODO: Wrappa med CV...

% training_labels = intressant subset;
% training_data = intressant subset;
% test_data = intressant subset

% test_labels = intressant subset;

%classifications = run_perceptron(training_data, training_labels, test_data);

%nErrors = sum(classifications ~= test_labels);